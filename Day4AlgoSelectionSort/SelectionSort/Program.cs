// selection sort programm

// пример из семинара
// исходник с gitHub

using static Sorting;
using static Infrastructure;

// int[] array = CreateArray(10);
// Show(array);
// SortSelection(array);
// Show(array);

10.CreateArray(min: 1, max: 10)
  .Show()
  .SortSelection()
  .Show();

